SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"
PROJECT_DIR=$SCRIPT_DIR/..
DOCS_DIR=$PROJECT_DIR/docs/
APP_DIR=$PROJECT_DIR/src/scrilla/

pdoc --html -o $DOCS_DIR $APP_DIR